FILE NOT FOUND FIX

The server now automatically looks for index.html in:
1. the same folder as server.ps1
2. the parent folder
3. the grandparent folder

Replace your old server.ps1 with this one.
Then double-click START_DOCLOCK_FIXED.bat.

Keep the PowerShell window open.
